spring:
  application:
    name: reviewer

app:
  branch-name: ${BRANCH_NAME:feature/core}
  received-tasks-path: ${RECEIVED_TASKS_PATH:classpath:csv/received/core}
  correct-tasks-path: ${CORRECT_TASKS_PATH:classpath:csv/correct/core}
